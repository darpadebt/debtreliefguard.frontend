/                          /index.html                                    200

# Blog list
/blog                      /blog.index.html                                200
/blog/                     /blog.index.html                                200

# Serve dated HTML files directly (workers commit real files into content/blog)
/blog/:date-:slug.html         /content/blog/:date-:slug.html               200

# Extensionless dated URLs → the actual .html file (one hop max)
/blog/:year-:month-:day-:rest        /content/blog/:year-:month-:day-:rest.html

# Non-dated slug .html requests → canonical extensionless slug loader
/blog/:slug.html               /blog/:slug                                   301
# Slug loader ONLY for extensionless slugs (never catches *.html)
/blog/:slug                /blog.slug.index.html                           200
/blog/:slug/               /blog.slug.index.html                           200

# Fallback: serve any other static artifact from content/blog
/blog/*                    /content/blog/:splat

# Allow direct access to raw content artifacts
/content/blog/*            /content/blog/:splat                           200


# Data & assets
/blogs.json                /blogs.json                                     200
/favicon.ico               /favicon.ico                                     200
/robots.txt                /robots.txt                                      200
/sitemap.xml               /sitemap.xml
/images/*                  /images/:splat                                   200
/assets/*                  /assets/:splat                                   200
